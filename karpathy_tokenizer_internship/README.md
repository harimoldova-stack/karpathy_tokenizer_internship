# Tiny Code GPT

A small GPT-style Transformer trained from scratch to generate Python code.

This project is an educational implementation of a tiny language model designed to demonstrate the main components of a GPT-style Transformer.

## Project Goal

The goal is to train a small Transformer on Python source code and generate code from partial function prompts.

For example:

```python
def multiply(a, b):